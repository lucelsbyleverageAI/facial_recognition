table:
  name: projects
  schema: public
array_relationships:
  - name: cards
    using:
      foreign_key_constraint_on:
        column: project_id
        table:
          name: cards
          schema: public
  - name: consent_profiles
    using:
      foreign_key_constraint_on:
        column: project_id
        table:
          name: consent_profiles
          schema: public
